package com.example.project.Model;

public class Operator extends User{
    public Operator(Integer uid, String username, String name, String email, String password, String role, Integer points) {
        super(uid, username, name, email, password, role, points);
    }
}
