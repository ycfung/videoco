package com.example.project.Model;

public class Admin extends User{
    public Admin(Integer uid, String username, String name, String email, String password, String role, Integer points) {
        super(uid, username, name, email, password, role, points);
    }
}
